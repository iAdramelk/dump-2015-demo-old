block('view-mode').content()(function () {
  var options = this.ctx.options;

  return [
    options.map(function (data) {
      return {
        elem: 'mode',
        content: {
          elem: 'link',
          mix: data.current ? [{block: 'is-current'}] : [],
          current: data.current,
          url: data.href,
          content: data.text
        }
      };
    })
  ];
});
