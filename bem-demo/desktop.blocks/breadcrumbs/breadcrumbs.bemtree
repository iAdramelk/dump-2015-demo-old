block('breadcrumbs').content()(function() {
    var options = this.ctx.options;

    return options.map(function (n) {
        return {
            elem: 'item',
            content: {
                elem: 'link',
                url: n.href,
                content: n.text
            }
        };
    });
});
